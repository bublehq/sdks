# Buble Dart and Flutter SDK Examples

Set `BUBLE_API_KEY` before running examples:

```bash
export BUBLE_API_KEY="sk_..."
```

Run an example:

```bash
dart run example/text_to_image.dart
```

Generation examples create real Buble generation tasks and may consume credits.
